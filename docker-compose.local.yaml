# Optional overlay for local development: expose Metabase on localhost:3000.
# Usage: docker compose -f docker-compose.yaml -f docker-compose.local.yaml up

services:
  metabase:
    ports:
      - "127.0.0.1:3000:3000"
